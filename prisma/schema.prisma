// git commit push
// prisma migrate dev --name nome_da_migracao
// npx prisma generate
// npx prisma db push
// vai atualizar no vercel e no neon

generator client {
  provider = "prisma-client-js"
  output   = "/node_modules/@prisma/client/"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Usuario {
  id        Int      @id @default(autoincrement())
  nome      String
  email     String   @unique
  senha     String
  createdAt DateTime @default(now())
  sessions  Sessao[]
}

model Sessao {
  id        String   @id
  userId    Int
  expiresAt DateTime

  user Usuario @relation(references: [id], fields: [userId], onDelete: Cascade)
}
